/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findConfigPda, findRegisterRoleUserRolePda } from "../pdas";
import { HARVVERSE_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";
import {
  getRoleKindDecoder,
  getRoleKindEncoder,
  type RoleKind,
  type RoleKindArgs,
} from "../types";

export const REGISTER_ROLE_DISCRIMINATOR = new Uint8Array([
  120, 142, 192, 194, 84, 145, 95, 167,
]);

export function getRegisterRoleDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_ROLE_DISCRIMINATOR,
  );
}

export type RegisterRoleInstruction<
  TProgram extends string = typeof HARVVERSE_PROGRAM_ADDRESS,
  TAccountUserRole extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUserRole extends string
        ? WritableAccount<TAccountUserRole>
        : TAccountUserRole,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterRoleInstructionData = {
  discriminator: ReadonlyUint8Array;
  role: RoleKind;
};

export type RegisterRoleInstructionDataArgs = { role: RoleKindArgs };

export function getRegisterRoleInstructionDataEncoder(): FixedSizeEncoder<RegisterRoleInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["role", getRoleKindEncoder()],
    ]),
    (value) => ({ ...value, discriminator: REGISTER_ROLE_DISCRIMINATOR }),
  );
}

export function getRegisterRoleInstructionDataDecoder(): FixedSizeDecoder<RegisterRoleInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["role", getRoleKindDecoder()],
  ]);
}

export function getRegisterRoleInstructionDataCodec(): FixedSizeCodec<
  RegisterRoleInstructionDataArgs,
  RegisterRoleInstructionData
> {
  return combineCodec(
    getRegisterRoleInstructionDataEncoder(),
    getRegisterRoleInstructionDataDecoder(),
  );
}

export type RegisterRoleAsyncInput<
  TAccountUserRole extends string = string,
  TAccountConfig extends string = string,
  TAccountUser extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  userRole?: Address<TAccountUserRole>;
  config?: Address<TAccountConfig>;
  user: TransactionSigner<TAccountUser>;
  systemProgram?: Address<TAccountSystemProgram>;
  role: RegisterRoleInstructionDataArgs["role"];
};

export async function getRegisterRoleInstructionAsync<
  TAccountUserRole extends string,
  TAccountConfig extends string,
  TAccountUser extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HARVVERSE_PROGRAM_ADDRESS,
>(
  input: RegisterRoleAsyncInput<
    TAccountUserRole,
    TAccountConfig,
    TAccountUser,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterRoleInstruction<
    TProgramAddress,
    TAccountUserRole,
    TAccountConfig,
    TAccountUser,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? HARVVERSE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    userRole: { value: input.userRole ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    user: { value: input.user ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.userRole.value) {
    accounts.userRole.value = await findRegisterRoleUserRolePda({
      user: expectAddress(accounts.user.value),
    });
  }
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.userRole),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterRoleInstructionDataEncoder().encode(
      args as RegisterRoleInstructionDataArgs,
    ),
    programAddress,
  } as RegisterRoleInstruction<
    TProgramAddress,
    TAccountUserRole,
    TAccountConfig,
    TAccountUser,
    TAccountSystemProgram
  >);
}

export type RegisterRoleInput<
  TAccountUserRole extends string = string,
  TAccountConfig extends string = string,
  TAccountUser extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  userRole: Address<TAccountUserRole>;
  config: Address<TAccountConfig>;
  user: TransactionSigner<TAccountUser>;
  systemProgram?: Address<TAccountSystemProgram>;
  role: RegisterRoleInstructionDataArgs["role"];
};

export function getRegisterRoleInstruction<
  TAccountUserRole extends string,
  TAccountConfig extends string,
  TAccountUser extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof HARVVERSE_PROGRAM_ADDRESS,
>(
  input: RegisterRoleInput<
    TAccountUserRole,
    TAccountConfig,
    TAccountUser,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterRoleInstruction<
  TProgramAddress,
  TAccountUserRole,
  TAccountConfig,
  TAccountUser,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? HARVVERSE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    userRole: { value: input.userRole ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    user: { value: input.user ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.userRole),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.user),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterRoleInstructionDataEncoder().encode(
      args as RegisterRoleInstructionDataArgs,
    ),
    programAddress,
  } as RegisterRoleInstruction<
    TProgramAddress,
    TAccountUserRole,
    TAccountConfig,
    TAccountUser,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterRoleInstruction<
  TProgram extends string = typeof HARVVERSE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    userRole: TAccountMetas[0];
    config: TAccountMetas[1];
    user: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: RegisterRoleInstructionData;
};

export function parseRegisterRoleInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterRoleInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      userRole: getNextAccount(),
      config: getNextAccount(),
      user: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterRoleInstructionDataDecoder().decode(instruction.data),
  };
}
