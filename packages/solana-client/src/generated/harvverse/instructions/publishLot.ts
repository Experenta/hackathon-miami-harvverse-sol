/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { HARVVERSE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const PUBLISH_LOT_DISCRIMINATOR = new Uint8Array([
  137, 26, 196, 178, 38, 125, 34, 246,
]);

export function getPublishLotDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(PUBLISH_LOT_DISCRIMINATOR);
}

export type PublishLotInstruction<
  TProgram extends string = typeof HARVVERSE_PROGRAM_ADDRESS,
  TAccountFarmer extends string | AccountMeta<string> = string,
  TAccountLot extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFarmer extends string
        ? ReadonlySignerAccount<TAccountFarmer> &
            AccountSignerMeta<TAccountFarmer>
        : TAccountFarmer,
      TAccountLot extends string ? WritableAccount<TAccountLot> : TAccountLot,
      ...TRemainingAccounts,
    ]
  >;

export type PublishLotInstructionData = { discriminator: ReadonlyUint8Array };

export type PublishLotInstructionDataArgs = {};

export function getPublishLotInstructionDataEncoder(): FixedSizeEncoder<PublishLotInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: PUBLISH_LOT_DISCRIMINATOR }),
  );
}

export function getPublishLotInstructionDataDecoder(): FixedSizeDecoder<PublishLotInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getPublishLotInstructionDataCodec(): FixedSizeCodec<
  PublishLotInstructionDataArgs,
  PublishLotInstructionData
> {
  return combineCodec(
    getPublishLotInstructionDataEncoder(),
    getPublishLotInstructionDataDecoder(),
  );
}

export type PublishLotInput<
  TAccountFarmer extends string = string,
  TAccountLot extends string = string,
> = {
  farmer: TransactionSigner<TAccountFarmer>;
  lot: Address<TAccountLot>;
};

export function getPublishLotInstruction<
  TAccountFarmer extends string,
  TAccountLot extends string,
  TProgramAddress extends Address = typeof HARVVERSE_PROGRAM_ADDRESS,
>(
  input: PublishLotInput<TAccountFarmer, TAccountLot>,
  config?: { programAddress?: TProgramAddress },
): PublishLotInstruction<TProgramAddress, TAccountFarmer, TAccountLot> {
  // Program address.
  const programAddress = config?.programAddress ?? HARVVERSE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    farmer: { value: input.farmer ?? null, isWritable: false },
    lot: { value: input.lot ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.farmer), getAccountMeta(accounts.lot)],
    data: getPublishLotInstructionDataEncoder().encode({}),
    programAddress,
  } as PublishLotInstruction<TProgramAddress, TAccountFarmer, TAccountLot>);
}

export type ParsedPublishLotInstruction<
  TProgram extends string = typeof HARVVERSE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    farmer: TAccountMetas[0];
    lot: TAccountMetas[1];
  };
  data: PublishLotInstructionData;
};

export function parsePublishLotInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedPublishLotInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { farmer: getNextAccount(), lot: getNextAccount() },
    data: getPublishLotInstructionDataDecoder().decode(instruction.data),
  };
}
