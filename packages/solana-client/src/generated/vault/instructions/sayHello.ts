/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
} from "@solana/kit";
import { VAULT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SAY_HELLO_DISCRIMINATOR = new Uint8Array([
  250, 163, 169, 119, 25, 42, 108, 31,
]);

export function getSayHelloDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SAY_HELLO_DISCRIMINATOR);
}

export type SayHelloInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      ...TRemainingAccounts,
    ]
  >;

export type SayHelloInstructionData = { discriminator: ReadonlyUint8Array };

export type SayHelloInstructionDataArgs = {};

export function getSayHelloInstructionDataEncoder(): FixedSizeEncoder<SayHelloInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SAY_HELLO_DISCRIMINATOR }),
  );
}

export function getSayHelloInstructionDataDecoder(): FixedSizeDecoder<SayHelloInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSayHelloInstructionDataCodec(): FixedSizeCodec<
  SayHelloInstructionDataArgs,
  SayHelloInstructionData
> {
  return combineCodec(
    getSayHelloInstructionDataEncoder(),
    getSayHelloInstructionDataDecoder(),
  );
}

export type SayHelloInput<TAccountSigner extends string = string> = {
  signer: TransactionSigner<TAccountSigner>;
};

export function getSayHelloInstruction<
  TAccountSigner extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: SayHelloInput<TAccountSigner>,
  config?: { programAddress?: TProgramAddress },
): SayHelloInstruction<TProgramAddress, TAccountSigner> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta(accounts.signer)],
    data: getSayHelloInstructionDataEncoder().encode({}),
    programAddress,
  } as SayHelloInstruction<TProgramAddress, TAccountSigner>);
}

export type ParsedSayHelloInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
  };
  data: SayHelloInstructionData;
};

export function parseSayHelloInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSayHelloInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { signer: getNextAccount() },
    data: getSayHelloInstructionDataDecoder().decode(instruction.data),
  };
}
